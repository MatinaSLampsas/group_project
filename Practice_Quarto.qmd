---
title: "Group Project"
author: "Katrina, Matina, Lindsey, Kaviya"
format: html
editor: visual
self-contained: true
---

```{r}
library(tidyverse)
library(janitor)


```

```{r}
library(tidyverse)

# Load in the life expectancy csv
life_exp <-read_csv(here::here("life_expectancy_at_birth.csv"))

#load in the carbon emissions data 
carbon <-read_csv(here::here("co2_emissions_tonnes_per_person.csv"))

```

When you click the **Render** button a document will be generated that includes both content and the output of embedded code. You can embed code like this:

```{r}
life_exp
```

You can add options to executable code like this

```{r}
#| echo: false
2 * 2
```

The `echo: false` option disables the printing of code (only output is displayed).
